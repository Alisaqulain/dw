export const metadata = {
  title: "Shop",
  description: "Browse premium body-safe silicone wellness products from TrustSilcon.",
};

export default function ShopLayout({ children }) {
  return children;
}
