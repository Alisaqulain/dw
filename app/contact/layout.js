export const metadata = {
  title: "Contact Us",
  description: "Contact TrustSilcon — email, phone, WhatsApp support for orders and product questions.",
};

export default function ContactLayout({ children }) {
  return children;
}
